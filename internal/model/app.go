package model

const (
	AppName        = "galaxy-merchant-trading"
	AppDescription = `Galaxy Merchant Trading is a command line tool 
for converting numbers and units from all over galaxy 
based on roman numerals.`
	AppUsage = "convert numbers and units"
)

type Param struct {
	File string
}
